# Generated by Anaconda 34.25.2.10
# Generated by pykickstart v3.32
#version=OL9
# Use graphical install
graphical
repo --name="AppStream" --baseurl=file:///run/install/sources/mount-0000-cdrom/AppStream

%addon com_redhat_kdump --enable --reserve-mb='auto'

%end

# Keyboard layouts
keyboard --xlayouts='us'
# System language
lang en_US.UTF-8

# Network information
network  --bootproto=static --device=eth0 --gateway=<DEFAULT_GW> --ip=<IP_ADDRESS> --nameserver=<DNS_SERVER> --netmask=<SUBNET_MASK> --ipv6=auto --activate
network  --hostname=<SERVER_NAME>

# Use CDROM installation media
cdrom

%packages
@^server-product-environment

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.6.0
ignoredisk --only-use=sda
# System bootloader configuration
bootloader --append="crashkernel=1G-64G:448M,64G-:512M" --location=mbr --boot-drive=sda
# Partition clearing information
clearpart --none --initlabel
# Disk partitioning information
part pv.223 --fstype="lvmpv" --ondisk=sda --size=1046950
part /boot --fstype="xfs" --ondisk=sda --size=1024
part /boot/efi --fstype="efi" --ondisk=sda --size=600 --fsoptions="umask=0077,shortname=winnt"
volgroup ol --pesize=4096 pv.223
logvol / --fstype="xfs" --size=1046948 --name=root --vgname=ol

timesource --ntp-pool=2.pool.ntp.org
# System timezone
timezone Asia/Jerusalem --utc

# Root password
rootpw --iscrypted --allow-ssh $6$0V1woc1ksq8.W3RN$bTTlHn41Tg2AwNw2X2vyDskxXSAtTbR8wRB59FwTDoBTzeh3.J4YkCQ1w9hEzG83JxqszYLRyjUli45uNfQU80

# Firewall configuration
firewall --disabled

# Reboot after installation
reboot